name: reuseable workflow
on:
    workflow_call:
        inputs:
            name:
                required: true
                
jobs:
    welcome:
        run-on: self-hosted
        steps:
        - name: welcome message
          run : echo "Hello" + ${{inputs.name}}
            